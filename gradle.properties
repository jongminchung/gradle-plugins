org.gradle.configuration-cache=true
org.gradle.parallel=true
org.gradle.caching=true

version=0.0.1

# Maven Central credentials (fill with your real values or override via CI secrets)
SONATYPE_HOST=CENTRAL_PORTAL
mavenCentralUsername=
mavenCentralPassword=

# Signing settings (signingKey must be the ASCII-armored secret exported via `gpg --export-secret-keys --armor`)
signing.keyId=
signing.password=
signing.secretKeyRingFile=
